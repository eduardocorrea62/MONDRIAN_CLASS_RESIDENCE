name: Notificar Telegram

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  send_message:
    runs-on: ubuntu-latest
    steps:
      - name: Enviar mensagens de boas-vindas para múltiplos usuários
        env:
          TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
        run: |
          MESSAGE="👋 Bem-vindo ao Suporte Técnico - MONDRIAN CLASS RESIDENCE!"
          
          # IDs dos usuários
          CHAT_IDS=("207223980" )  # <-- Substituir pelos IDs reais

          for CHAT_ID in "${CHAT_IDS[@]}"
          do
            echo "Enviando mensagem para o chat_id $CHAT_ID"
            curl -s -X POST "https://api.telegram.org/bot${TELEGRAM_TOKEN}/sendMessage" \
              -d chat_id="$CHAT_ID" \
              -d text="$MESSAGE"
          done

